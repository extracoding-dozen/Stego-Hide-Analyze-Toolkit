name: Build Stego Toolkit

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            output_name: stego-toolkit-linux
            asset_ext: ""
          - os: windows-latest
            output_name: stego-toolkit-windows
            asset_ext: ".exe"
          - os: macos-latest
            output_name: stego-toolkit-macos
            asset_ext: ""

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    # Установка зависимостей для Linux (необходимы для GioUI)
    - name: Install Linux Dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y libwayland-dev libx11-dev libx11-xcb-dev libxkbcommon-x11-dev libgles2-mesa-dev libegl1-mesa-dev

    - name: Tidy and Download modules
      run: |
        go mod tidy
        go mod download

    - name: Build
      run: |
        go build -v -o build/${{ matrix.output_name }}${{ matrix.asset_ext }} ./cmd/main.go

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: release-${{ matrix.output_name }}
        path: build/${{ matrix.output_name }}${{ matrix.asset_ext }}